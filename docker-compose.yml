version: '3.8'
services:
  web:
    env_file:
      - .env
    build:
      context: ./
      target: production
    volumes:
      - .:/app
    command: node build/index.js
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production